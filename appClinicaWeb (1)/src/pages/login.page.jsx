import { LoginContenedor } from "../contenedores/usuarios/login";
import { Layout } from "../tema/layout";

export const LoginPage = () => {
    return (
        <Layout>
            <LoginContenedor />
        </Layout>
    );
};